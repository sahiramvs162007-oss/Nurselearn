<header class="topbar">
  <div class="topbar-portal-title">SENA Nursing English</div>
  <div class="topbar-right">
    <button class="topbar-icon-btn" title="Notificaciones">
      <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/>
        <path d="M13.73 21a2 2 0 0 1-3.46 0"/>
      </svg>
    </button>

    <div class="user-avatar-pill">
      <% const initials = (locals.user || 'User').split(' ').map(n => n[0]).join('').substring(0,2).toUpperCase(); %>
      <div class="user-avatar-circle"><%= initials %></div>
      <div class="user-avatar-info">
        <span class="user-avatar-name"><%= locals.user || 'Usuario' %></span>
        <span class="user-avatar-rol"><%= locals.userRol || 'NurseLearn' %></span>
      </div>
    </div>
  </div>
</header>
